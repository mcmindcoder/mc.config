This gdb pulgin allows to print Eigen matrix in readable format in gdb

Installation:

Update the path to current dir inside _.gdbinit file

Copy _.gdbinit to user home folder without '_' prefix
cp _.gdbinit ~/.gdbinit