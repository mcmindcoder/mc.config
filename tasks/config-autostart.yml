---

- name: 'Create folder ~/.config/autostart'
  file:
    path: ~/.config/autostart
    state: directory
    owner: '{{user}}'
    mode: u=rwx,g=rwx,o-rwx

- name: 'Copy autostart launchers'
  become_user: '{{user}}'
  copy:
    src: '{{item}}'
    dest: ~/.config/autostart
    owner: '{{user}}'
    mode: u=rw,g=rw,o-rwx
  with_fileglob: '{{base_path}}/files/autostart/*'
